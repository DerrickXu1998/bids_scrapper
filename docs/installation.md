# Installation

## Stable release

To install bids_scraper, run this command in your terminal:

```sh
uv add python-boilerplate
```

Or if you prefer to use `pip`:

```sh
pip install python-boilerplate
```

## From source

The source files for bids_scraper can be downloaded from the [Github repo](https://github.com/DerrickXu1998/python_boilerplate).

You can either clone the public repository:

```sh
git clone git://github.com/DerrickXu1998/python_boilerplate
```

Or download the [tarball](https://github.com/DerrickXu1998/python_boilerplate/tarball/master):

```sh
curl -OJL https://github.com/DerrickXu1998/python_boilerplate/tarball/master
```

Once you have a copy of the source, you can install it with:

```sh
cd python_boilerplate
uv pip install .
```
