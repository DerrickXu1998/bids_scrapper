services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
      - SELENIUM_REMOTE_URL=http://host.docker.internal:4444
    volumes:
      - .:/app
